# === Tweequick root .env (backend/Lambdas/DB) ===

# --- Twitter API ---
TWITTER_BEARER_TOKEN=

# --- MET (Malaysian Meteorological Department) ---
MET_GOV_KEY=

# --- PostgreSQL / Aurora ---
PGHOST=
PGPORT=5432
PGDATABASE=
PGUSER=
PGASSWORD=
# SSL mode: disable | require | verify-ca | verify-full
PGSSLMODE=disable
# Optional SSL files (paths). Common for RDS: rds-combined-ca-bundle.pem
PGSSLROOTCERT=
PGSSLCERT=
PGSSLKEY=
PGSSLPASSWORD=

# Optional: Table overrides
DB_TABLE_TOWNS=public.towns

# --- Amazon Bedrock (optional; if you refactor code to read from env) ---
BEDROCK_REGION=us-east-1
BEDROCK_AGENT_ID=
BEDROCK_AGENT_ALIAS_ID=

# --- Google APIs (server-side Routes/Geocode) ---
GOOGLE_API_KEY=

# --- Hazard radii in meters (server-side filtering) ---
HAZARD_RADIUS_LOW=1500
HAZARD_RADIUS_MEDIUM=3000
HAZARD_RADIUS_HIGH=6000
HAZARD_RADIUS_CRITICAL=10000

# --- Local helpers ---
# Set to 1 to let DB-Setup/db.py attempt a test connection when run directly
DB_TEST_CONNECT=
#Set up for AmplifyNextJS
NEXT_PUBLIC_GM_API = 

## LambdaFunctions

# Set up for MetData
MET_GOV_KEY = 

# Set up for Polyline
GOOGLE_API_KEY = 

#Set up for weather_data.py
VISUAL_CROSSING_KEY = 

## DB Setup
PGHOST=
PGPORT=
PGUSER=
PGPASSWORD=
PGDATABASE=
PGSSLMODE=require